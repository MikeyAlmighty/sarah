:PROPERTIES:
:ID:       9418ae56-d4dd-46e3-b7da-c404bc567e5e
:END:
#+title: While Loops
#+filetags: :OCP:
#+property: header-args :tangle code/ocp/3/while_loops.java

* While Loops

A ~loop~ is a repetitive control structure that can execute a statement of code *multiple* *times* in succession.

By using =variables= that can be assigned new values, each repetition of the statement may be different.

** Syntax

#+BEGIN_SRC java
while (loop-continuation-condition) {
    //  Loop body
    Statement(s);
}
#+END_SRC

#+BEGIN_SRC java
int roomInBelly = 5;
public void eatCheese(int bitesOfCheese) {
    while (bitesOfCheese > 0 && roomInBelly > 0) {
        bitesOfCheese-­;
        roomInBelly--;
    }
System.out.println(bitesOfCheese + " pieces of cheese left");
}
#+END_SRC

** Note
The *loop-continuation-condition* must always appear inside the =parentheses=.
The braces enclosing the loop body can be *omitted* only if the loop body contains *one* or *no* statement.

** [[id:29e84bbe-e632-45ff-89d4-32c0c43478d6][Sentinel-Controlled While Loop]]
** [[id:ba4935c1-905d-4132-aa61-62a8c1b54065][Reading From File]]

* [[id:9beebbaf-ee31-4d85-a6ff-5cc2eae2d964][Do While Loops]]
