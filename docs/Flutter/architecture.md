# Architecture

One codebase but provides platform specific capabilities.

Everything is a widget

UI as Code

## Compilation

Flutter API => Dart Code => Flutter SDK => Native Platform

Flutter does *NOT* use Platform Primitaves. Flutter uses it's own Engine.

Material Design for Android (Can customized with own styling).
IOS Cupertino also avaialable.







