:PROPERTIES:
:ID:       f6e458df-99cf-45d6-b224-16f325cc1d2b
:END:
#+title: Arithmetic Operators
#+filetags: :OCP:

* Arithmetic Operators
A [[id:9bcf2aef-060a-4011-98b5-cb949a9f7d1a][Binary Operator]] that operate on [[id:2483364b-73d3-4169-92b9-1fea473a9b01][numeric values]].

#+ATTR_ORG: :width 700
[[./assets/arithmetic_operators.png]]

Applied to *all* [[id:46582863-7f97-448d-bbc7-f7da4c1e7574][Primitive Types]] *except* for ~Boolean~

Only the addition operators ~+~ and ~+=~ may be applied to *String* values, which results in =String concatenation=.

** Division
When both operands of a *division* are ~integers~, the result of the *division* is the =quotient= and the =fractional= part is *truncated*.
For example, ~5 / 2~ yields =2=, not =2.5=, and ~–5 / 2~ yields =–2=, not =–2.5=.

To perform a *floating-point division*, one of the operands must be a *floating-point* number.
For example, ~5.0 / 2~ yields =2.5=.

*** Modulus
The =%= operator, known as *remainder*, yields the *remainder* after division.

The operand on the left is the *dividend*, and the operand on the right is the *divisor*.

Therefore, ~7 % 3~ yields =1=, ~3 % 7~ yields =3=, ~12 % 4~ yields =0=, ~26 % 8~ yields =2=, and ~20 % 13~ yields =7=.
#+ATTR_ORG: :width 700
[[./assets/division-operator.png]]


See also:
- [[id:be91e553-3d67-4440-ab4e-e18e77b5c48a][Binary Numeric Promotion]]
