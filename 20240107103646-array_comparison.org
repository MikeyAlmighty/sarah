:PROPERTIES:
:ID:       d06d8e67-0233-4275-8eaa-f0660d5b6cf1
:END:
#+title: Array Comparison
#+property: header-args :tangle code/ocp/4/array_comparison.java

* Array Comparison

Java also provides methods to *compare* two arrays to determine which is *smaller*.

** Using ~.compare()~

- A *negative* number means the *first* array is *smaller* than the *second*.
- A *zero* means the arrays =are equal=.
- A *positive* number means the *first* array is *larger* than the *second*.

*** Example (Single length)

#+BEGIN_SRC java
System.out.println(Arrays.compare(new int[] {1}, new int[] {2}));
#+END_SRC

*** Example (Multi length)

- If *both* arrays are the *same length* and have the *same values* in each spot in the *same order*, return =zero=.

- If *all* the elements are the *same* but the *second* array has extra elements at the end, return a =negative number=.

- If *all* the elements are the *same*, but the *first* array has extra elements at the end, return a =positive number=.

- If the *first* element that differs is *smaller* in the first array, return a =negative number=.

- If the *first* element that differs is *larger* in the first array, return a =positive number=.

*** What does smaller mean?

- ~null~ is smaller than any other value.

- For ~numbers~, normal numeric order applies.

- For ~strings~, one is smaller if it is a *prefix of another*.

- For ~strings/characters~, *numbers* are smaller than *letters*.

- For ~strings/characters~, *uppercase* is smaller than *lowercase*.

*** Examples

#+ATTR_ORG: :width 700
[[./assets/array_compare_examples.png]]

**** _Exam Trick_

Finally, this code does not compile because the *types are different*.
When comparing two arrays, they *must* be the *same array type*.

#+BEGIN_SRC java
System.out.println(Arrays.compare(new int[] {1}, new String[] {"a"})); // DOES NOT COMPILE
#+END_SRC
